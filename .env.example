# ============================================
# Kindle Reading GTK 云同步服务端 - 环境变量配置示例
# ============================================

# ============================================
# 应用配置
# ============================================
APP_NAME="Kindle Reading GTK"
APP_ENV=production
APP_DEBUG=false
APP_URL=https://your-domain.com

# ============================================
# 数据库配置
# ============================================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=kindle_reading
DB_USER=your_db_user
DB_PASS=your_db_password
DB_CHARSET=utf8mb4

# ============================================
# GitHub OAuth 配置（预留字段）
# ============================================
# 注意：Kindle 端不需要配置这些字段，只需在服务端配置
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
GITHUB_REDIRECT_URI=https://your-domain.com/api/callback.php

# ============================================
# 文件上传配置
# ============================================
# 最大上传文件大小（字节），默认 100MB
MAX_FILE_SIZE=104857600
# 允许上传的文件扩展名（逗号分隔）
ALLOWED_EXTENSIONS=log,txt
# 上传文件存储路径（相对于项目根目录）
UPLOAD_PATH=public/uploads

# ============================================
# 会话配置
# ============================================
# OAuth 会话超时时间（秒），默认 5 分钟
SESSION_TIMEOUT=300
# 用户访问令牌有效期（秒），默认 2 小时
USER_TOKEN_LIFETIME=7200

# ============================================
# 日志配置
# ============================================
# 日志级别：debug, info, warning, error
LOG_LEVEL=info
# 日志文件存储路径（相对于项目根目录）
LOG_PATH=storage/logs

# ============================================
# 安全配置
# ============================================
# 加密密钥（用于加密敏感数据，请修改为随机字符串）
ENCRYPTION_KEY=your-secret-encryption-key-change-this
# 会话令牌前缀
SESSION_TOKEN_PREFIX=kr_
# 用户令牌前缀
USER_TOKEN_PREFIX=ur_